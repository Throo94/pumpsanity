import 'package:flutter/material.dart';

class PatchnoteEngine extends StatelessWidget {
  const PatchnoteEngine({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
