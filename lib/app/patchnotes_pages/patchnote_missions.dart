import 'package:flutter/material.dart';

class PatchnoteMissions extends StatelessWidget {
  const PatchnoteMissions({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
